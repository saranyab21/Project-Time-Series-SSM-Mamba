name: gait_mamba
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - numpy
  - pandas
  - scikit-learn
  - matplotlib
  - seaborn
  - scipy
  - pip
  - pip:
      - tsfresh
      - tqdm
      - pyarrow
      - jupyterlab

